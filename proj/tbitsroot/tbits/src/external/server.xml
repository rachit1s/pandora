<?xml version="1.0" encoding="UTF-8"?>
<!--
  Licensed to the Apache Software Foundation (ASF) under one or more
  contributor license agreements.  See the NOTICE file distributed with
  this work for additional information regarding copyright ownership.
  The ASF licenses this file to You under the Apache License, Version 2.0
  (the "License"); you may not use this file except in compliance with
  the License.  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<!-- Example Server Configuration File -->
<!-- Note that component elements are nested corresponding to their
     parent-child relationships with each other -->

<!-- A "Server" is a singleton element that represents the entire JVM,
     which may contain one or more "Service" instances.  The Server
     listens for a shutdown command on the indicated port.

     Note:  A "Server" is not itself a "Container", so you may not
     define subcomponents such as "Valves" or "Loggers" at this level.
 -->

<Server port="8081" shutdown="SHUTDOWN">
	<!-- Comment these entries out to disable JMX MBeans support used for the 
       administration web application -->
  <!--<Listener className="org.apache.catalina.core.AprLifecycleListener" />
  <Listener className="org.apache.catalina.mbeans.ServerLifecycleListener" />
  <Listener className="org.apache.catalina.mbeans.GlobalResourcesLifecycleListener" />
  <Listener className="org.apache.catalina.storeconfig.StoreConfigLifecycleListener"/>-->

  <!-- Global JNDI resources -->
  <GlobalNamingResources>
  <!-- Test entry for demonstration purposes -->
    <!--Environment name="simpleValue" type="java.lang.Integer" value="30"/-->

    <!-- Editable user database that can also be used by
         UserDatabaseRealm to authenticate users -->
    <Resource name="UserDatabase" auth="Container"
              type="org.apache.catalina.UserDatabase"
       description="User database that can be updated and saved"
           factory="org.apache.catalina.users.MemoryUserDatabaseFactory"
          pathname="conf/tomcat-users.xml" />

   <Resource 
   		name="masterPool" 
		auth="Container"
              	type="java.util.ArrayList" 
       		description="Holds all the pools" 
		factory="uk.org.primrose.pool.datasource.MasterPoolDataSourceFactory"
		configFile="${build.server.Resource.masterpool.configfile}"
    />

    <Resource 
    		name="tbitsPool" 
		auth="Container"
              	type="uk.org.primrose.pool.datasource.PoolDataSource" 
       		description="The tbits database pool"  
		factory="uk.org.primrose.pool.datasource.PoolDataSourceFactory"
       		poolName="tbitsPool" 
    />

    <!--Resource 
    		name="ctsPool" 
		auth="Container"
              	type="uk.org.primrose.pool.datasource.PoolDataSource" 
       		description="The CTS database pool"  
		factory="uk.org.primrose.pool.datasource.PoolDataSourceFactory"
       		poolName="ctsPool" 
    />

    <Resource name="fundRunnerPool" auth="Container"
              	type="uk.org.primrose.pool.datasource.PoolDataSource" 
       		description="The FundRunner database pool"  
		factory="uk.org.primrose.pool.datasource.PoolDataSourceFactory"
       		poolName="fundRunnerPool" 
    /-->

  </GlobalNamingResources>
  
  <!-- A "Service" is a collection of one or more "Connectors" that share
       a single "Container" (and therefore the web applications visible
       within that Container).  Normally, that Container is an "Engine",
       but this is not required.

       Note:  A "Service" is not itself a "Container", so you may not
       define subcomponents such as "Valves" or "Loggers" at this level.
   -->

  <!-- Define the Tomcat Stand-Alone Service -->
	<Service name="Catalina">
	<!-- A "Connector" represents an endpoint by which requests are received
         and responses are returned.  Each Connector passes requests on to the
         associated "Container" (normally an Engine) for processing.

         By default, a non-SSL HTTP/1.1 Connector is established on port 8080.
         You can also enable an SSL HTTP/1.1 Connector on port 8443 by
         following the instructions below and uncommenting the second Connector
         entry.  SSL support requires the following steps (see the SSL Config
         HOWTO in the Tomcat 5 documentation bundle for more detailed
         instructions):
         * If your JDK version 1.3 or prior, download and install JSSE 1.0.2 or
           later, and put the JAR files into "$JAVA_HOME/jre/lib/ext".
         * Execute:
             %JAVA_HOME%\bin\keytool -genkey -alias tomcat -keyalg RSA (Windows)
             $JAVA_HOME/bin/keytool -genkey -alias tomcat -keyalg RSA  (Unix)
           with a password value of "changeit" for both the certificate and
           the keystore itself.

         By default, DNS lookups are enabled when a web application calls
         request.getRemoteHost().  This can have an adverse impact on
         performance, so you can disable it by setting the
         "enableLookups" attribute to "false".  When DNS lookups are disabled,
         request.getRemoteHost() will return the String version of the
         IP address of the remote client.
    -->
	<Connector 
	           port="8443" maxThreads="200"
	           scheme="https" secure="true" SSLEnabled="true"
	           keystoreFile="keystore" keystorePass="sandeep"
	           clientAuth="false" sslProtocol="TLS" />
		<Connector 	
			
			port="${build.transbit.tbits.port}" 
			maxThreads="150" 
			minSpareThreads="25" 
			maxSpareThreads="75" 
			enableLookups="false"  
			redirectPort="8443"
			acceptCount="100" 
			connectionTimeout="600000"  
			disableUploadTimeout="true" 
			URIEncoding="UTF-8"
			compressableMimeType="text/javascript,text/css,text/html,text/xml,text/plain"
			compression="on"
			socket_keepalive="true"
			server='tBits 6.0'
		/>
		<Engine 
			name="Catalina" 
			defaultHost="localhost">
			<Realm className="org.apache.catalina.realm.UserDatabaseRealm"
             resourceName="UserDatabase"/>
			<Host 
				name="localhost" 
				appBase="webapps" 
				unpackWARs="true" 
				autoDeploy="true" 
				xmlValidation="false"  
				xmlNamespaceAware="false">
				<Context 
					path="" 
					reloadable="true"
					docBase="../../build/webapps">
					<Manager 
						className="org.apache.catalina.session.PersistentManager" 
						distributable="false" 
						saveOnRestart="true"
					>
						<Store
							className="org.apache.catalina.session.FileStore"
						/>
					</Manager>

					  <Valve className="waffle.apache.NegotiateAuthenticator" principalFormat="fqn" roleFormat="both" />
					  <Realm className="waffle.apache.WindowsRealm" />

					<Valve 
						className="org.apache.catalina.valves.FastCommonAccessLogValve"
						directory="${build.transbit.tbits.logdir}"
						pattern="common"
						prefix="access_log_"
						suffix=".log"
						rotatable="true"
						fileDateFormat="yyyy-MM-dd"
					/>
		    			<!--ResourceLink
						global="ctsPool"
						name="ctsPool"
						type="javax.sql.DataSource"/>
		    			<ResourceLink
						global="fundRunnerPool"
						name="fundRunnerPool"
						type="javax.sql.DataSource"/-->
		    			<ResourceLink
						global="tbitsPool"
						name="tbitsPool"
						type="javax.sql.DataSource"/>
		    			<ResourceLink
						global="masterPool"
						name="masterPool"
						type="java.util.ArrayList"/>

				</Context>
				<Context 
					path="/reports" 
					reloadable="true"
					docBase="../../build/webapps/reports">
					<Manager 
						className="org.apache.catalina.session.PersistentManager" 
						distributable="false" 
						saveOnRestart="false"
					/>
					<Valve 
						className="org.apache.catalina.valves.FastCommonAccessLogValve"
						directory="/opt/tbits/logs"
						pattern="common"
						prefix="access_log_"
						suffix=".log"
						rotatable="true"
						fileDateFormat="yyyy-MM-dd"
					/>

				</Context>


<!--               	<Context 
					path="/web" 
					docBase="/opt/tbits/build/webapps/web">
					<Manager 
						className="org.apache.catalina.session.PersistentManager" 
						distributable="false" 
						saveOnRestart="false"
					/>
					<Valve 
						className="org.apache.catalina.valves.FastCommonAccessLogValve"
						directory="/opt/tbits/logs"
						pattern="common"
						prefix="access_log_"
						suffix=".log"
						rotatable="true"
						fileDateFormat="yyyy-MM-dd"
					/>
				</Context>-->
			</Host>
		</Engine>
	</Service>
</Server>
